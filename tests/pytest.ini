[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --import-mode=importlib

markers =
    unit: Pure logic tests with no external dependencies
    integration: Tests that load real modules with stubs
